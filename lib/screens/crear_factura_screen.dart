import 'package:flutter/material.dart';

class CrearFacturaScreen extends StatelessWidget {
  const CrearFacturaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CrearFacturaScreen'),
      ),
    );
  }
}
