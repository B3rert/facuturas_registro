import 'package:flutter/material.dart';

class AgrupadoScreen extends StatelessWidget {
  const AgrupadoScreen({Key? key}) : super(key: key);
  //name route
  static const routeName = 'agrupado';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AgrupadoScreen'),
      ),
    );
  }
}
